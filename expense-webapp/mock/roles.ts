// specs/design/security.json → roles[].name
// A single role: every signed-in household member sees the same screens, so
// there is nothing to switch between — this is the roster's whole content.
export const mockRoles = ["Household Member"];
